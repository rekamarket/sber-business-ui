---

title: SberButton


---

# SberButton

## size

```tsx
import React from 'react'
import { SberButton } from '@rekamarket/sber-business-ui'

export default () => (<div style={{ display: "grid", gap: 10 }}>
	<SberButton size="sm">
		Кнопка
	</SberButton>
	<SberButton size="md">
		Кнопка
	</SberButton>
	<SberButton size="lg">
		Кнопка
	</SberButton>
	<SberButton size="xlg">
		Кнопка
	</SberButton>
</div>)
```

## variant

```tsx
import React from 'react'
import { SberButton } from '@rekamarket/sber-business-ui'

export default () => (<div style={{ display: "grid", gap: 10 }}>
	<SberButton variant="general">
		Кнопка
	</SberButton>
	<SberButton variant="secondary">
		Кнопка
	</SberButton>
</div>)
```
